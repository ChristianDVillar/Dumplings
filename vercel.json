{
  "version": 2,
  "buildCommand": "npm install && npx expo export:web && (test -d web-build && mv web-build dist || true)",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/debug.html"
    }
  ]
}
